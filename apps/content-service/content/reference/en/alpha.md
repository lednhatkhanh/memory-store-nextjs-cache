# Alpha field note

Alpha is published from a plain Markdown file and delivered through the shared Redis cache.

## What this demonstrates

- Editors can change content without touching application code.
- The content path defines its site, locale, and slug.
- Each file receives a revision derived from its contents.
