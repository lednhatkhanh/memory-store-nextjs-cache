# Beta release brief

Beta uses the same content pipeline with a different slug, so navigation exercises an independent cache entry.

**Markdown stays the authoring format** while the content service keeps the public response typed and predictable.
